# Copy to `.env` and fill in. `.env` is gitignored — never commit real keys.

# Roboflow API key (app.roboflow.com -> Settings -> API Keys)
ROBOFLOW_API_KEY=

# HuggingFace read token (huggingface.co/settings/tokens) — for SigLIP team model
HF_TOKEN=
