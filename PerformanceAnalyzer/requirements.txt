# PerformanceAnalyzer — object detection/tracking (CV) + analytics layer
# Pin for Python 3.10+; tested on 3.11/3.12.

# Core numerical / image
numpy>=1.26
pandas>=2.0
opencv-python>=4.9

# Machine learning runtime + object detection
torch>=2.2
torchvision>=0.17
ultralytics>=8.2

# Tracking & annotation toolkit (ByteTrack, Detections, KeyPoints)
# Pinned <0.30: sv.ByteTrack is deprecated after 0.28 and removed in 0.30.
supervision>=0.23,<0.30

# Team classification (SigLIP -> UMAP -> KMeans)
transformers>=4.40
umap-learn>=0.5
scikit-learn>=1.3

# Analytics layer (xG, VAEP, pitch control/EPV, passing networks, radar)
matplotlib>=3.8
networkx>=3.0
scipy>=1.12

# Misc
tqdm
pyyaml
requests
# Dev / test
pytest>=8.0

# Optional: hosted Roboflow detector + .env loading
inference>=0.17
python-dotenv>=1.0

# Optional analytics extras
soccerdata>=1.7   # FBref / Understat / WhoScored / SofaScore / ESPN scrapers